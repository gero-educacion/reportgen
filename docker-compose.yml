version: "3.9"

services:
  reportgen:
    build: .
    ports:
      - "8080:8080"

    env_file:
      - .env

    volumes:
      - ./secrets:/secrets:ro
      - ./tmp:/tmp
      - ./data:/app/data      # ← persistent historic + lock file

    restart: unless-stopped